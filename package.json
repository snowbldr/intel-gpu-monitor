{
  "name": "intel-gpu-monitor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": "index.js",
  "type": "module",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "docker:build": "docker compose build",
    "docker:run": "docker compose up -d --force-recreate --build"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@questdb/nodejs-client": "^3.0.0",
    "command-exists": "^1.2.9",
    "commander": "^13.0.0"
  }
}
